# Public env example. Copy to .env.local and edit.
# Values are baked into the bundle at `next build` time.

# Base URL for REST calls
NEXT_PUBLIC_API_BASE=https://localhost:8443/api/v1

# Base URL for the realtime WebSocket
NEXT_PUBLIC_WS_URL=wss://localhost:8443/ws
